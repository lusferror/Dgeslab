const funcion = (getStore, getActions, setStore)=>{

    return{

        store:{//Remember to put the prefix "form" for the variables
    
        },
    
        actions:{// Remembser to put the suffix "form" for the functions
    
        }
    }
    
}

export default funcion;